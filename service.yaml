apiVersion: v1
kind: Service

metadata:
  name: sudoku-service

spec:
  type: NodePort

  selector:
    app: sudoku-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
